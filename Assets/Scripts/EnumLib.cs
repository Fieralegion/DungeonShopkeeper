using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace EnumLib
{
    public enum itemType { Weapon, Potion, Shield};
    public enum textType { Greeting, Depart}
    public enum conditionals { None, Item, ItemInFront, Money}
    public enum resutls { Sell, Buy, Item}
}
